# dependencies
node_modules/

# environment — secrets never enter the repo
.env
.env*.local

# builds
dist/
.next/
out/
*.tsbuildinfo
coverage/

# misc
*.log
.DS_Store

# vercel CLI project link (local machine only, not secret but not portable)
.vercel/
