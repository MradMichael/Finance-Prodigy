# SQL Server connection string (Prisma sqlserver provider — see prisma/schema.prisma)
DATABASE_URL="sqlserver://HOST:PORT;database=essa;integratedSecurity=true;trustServerCertificate=true"
PORT=4000
CLIENT_ORIGIN=http://localhost:3000
